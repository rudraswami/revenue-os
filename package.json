{
  "name": "revenue-os",
  "version": "0.1.0",
  "private": true,
  "description": "AI Revenue Operating System for WhatsApp-first businesses",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "dev:api": "turbo run dev --filter=@revenue-os/api",
    "dev:web": "turbo run dev --filter=@revenue-os/web",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md}\"",
    "db:generate": "pnpm --filter @revenue-os/database generate",
    "db:migrate": "dotenv -e .env -- pnpm --filter @revenue-os/database migrate:dev",
    "db:push": "dotenv -e .env -- pnpm --filter @revenue-os/database push",
    "db:auth-migrate": "dotenv -e .env -- node scripts/migrate-auth-production.js",
    "db:deploy": "dotenv -e .env -- pnpm --filter @revenue-os/database migrate:deploy",
    "db:seed": "dotenv -e .env -- pnpm --filter @revenue-os/database seed",
    "db:studio": "dotenv -e .env -- pnpm --filter @revenue-os/database studio",
    "supabase": "supabase",
    "supabase:login": "supabase login",
    "supabase:link": "supabase link",
    "supabase:push": "supabase db push",
    "supabase:pull": "supabase db pull",
    "supabase:status": "supabase status",
    "supabase:types": "supabase gen types typescript --linked -o packages/shared/src/supabase.types.ts",
    "setup:supabase": "powershell -ExecutionPolicy Bypass -File scripts/supabase/setup.ps1",
    "setup:all": "pnpm setup:supabase && pnpm db:push && pnpm db:seed",
    "setup:local": "powershell -ExecutionPolicy Bypass -File scripts/setup-local.ps1",
    "docker:up": "docker compose -f docker/docker-compose.yml up -d",
    "docker:down": "docker compose -f docker/docker-compose.yml down",
    "prepare": "husky || true",
    "postinstall": "pnpm db:generate"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "dotenv-cli": "^8.0.0",
    "husky": "^9.1.7",
    "pg": "^8.21.0",
    "prettier": "^3.4.2",
    "supabase": "^2.2.1",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  }
}
