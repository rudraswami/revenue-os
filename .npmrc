# Vercel sets NODE_ENV=production during install, which skips devDependencies.
# Workspace packages (@growvisi/shared, @growvisi/database) need typescript/prisma at build time.
# installCommand also sets NODE_ENV=development — this is a fallback for local prod installs.
include-workspace-root=true
